using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DebugAi : MonoBehaviour
{
   
    // basic - classic (returns to given waypoint when failed movement)
    // Shy - cant move until not observed on camera. - foxy
    // technophobe - any camera usage resets them.
    // Sneaky - does not move back when stopped - freddy
    // nuisance - 



/*
    Attempt move Logic
        {
        rand number 1-20
        if =||greater than AI level (attempt move)
        }

     thinking

      - wait timer (rest time) (actions performed during)
      - attempt Move
      
        -true-
      -  move next waypoint
      - jumpscare at end of chain

        -false-
        repeat

    */



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
